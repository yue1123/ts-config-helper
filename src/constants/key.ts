import { version } from '@package'

export const SETTING_STORAGE = `ts-helper:setting_storage:${version}`
export const DATA_CACHE = `ts-helper:data_cache:${version}`
export const THEME = `ts-helper:theme:${version}`
export const RUN_TIME = 'ts-helper:runtime'
export const GUIDE = 'ts-helper:guide'
